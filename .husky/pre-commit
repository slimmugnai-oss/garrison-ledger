#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# PRETTIER AUTO-FORMAT DISABLED (Oct 2025)
# To enable: Uncomment the line below
# npx lint-staged

# For now, only the existing pre-commit checks run
# See: docs/PRETTIER_SETUP_READY.md for details
